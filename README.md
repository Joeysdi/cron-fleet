# cron-fleet

Scheduled HTTP pings for [linksgone.com](https://www.linksgone.com) background
jobs. Every endpoint requires a Bearer secret and returns 401 without it — this
repository contains no application code, only the schedule.

Public so the schedule runs on GitHub's unlimited free minutes for public
repositories.
